using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PoopyFart : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //This is where the poop starts?
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
